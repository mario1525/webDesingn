CREATE DATABASE mensajes;

CREATE Table datos (id int(11) AUTO_INCREMENT,
                    nombre varchar(50), 
                    correo varchar(50), 
                    mensaje varchar(255),
                     PRIMARY key(id) );