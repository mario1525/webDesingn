create DATABASE formulario;

use DATABASE formulario;

CREATE Table datos (nombre varchar(50), 
                    correo varchar(50), 
                    sexo varchar(50),
                    id int PRIMARY key )

